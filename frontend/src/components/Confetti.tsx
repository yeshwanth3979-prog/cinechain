/* Simple Confetti component placeholder (a real one could use react-confetti) */
export default function Confetti() {
    return null; // we'll skip actual canvas confetti to keep dependencies light, the CSS glow effects handle the celebration!
}
